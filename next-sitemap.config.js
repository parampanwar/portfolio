/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://www.parampanwar.com',
    generateRobotsTxt: true, // This creates the robots.txt for you too!
    // optional
    generateIndexSitemap: false,
}